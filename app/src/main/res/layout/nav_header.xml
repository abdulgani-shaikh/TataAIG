<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/default_background_color"
    android:paddingHorizontal="@dimen/business_card_inner_child_horizontal_padding"
    android:paddingVertical="@dimen/large_padding_card">

    <ImageView
        android:id="@+id/closeImgNav"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:scaleType="center"
        android:src="@drawable/ic_close"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/profileImgNav"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginVertical="@dimen/business_card_inner_child_vertical_padding"
        android:backgroundTint="@color/dark_blue_color"
        android:src="@drawable/img_default_avatar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/CircularImageView" />

    <TextView
        android:id="@+id/usernameNav"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="10dp"
        android:text="Baseer Singh"
        android:textColor="@color/dark_blue_color"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@id/profileImgNav"
        app:layout_constraintTop_toTopOf="@id/profileImgNav" />

    <TextView
        android:id="@+id/viewProfileNav"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppins_medium"
        android:paddingHorizontal="10dp"
        android:text="View Profile"
        android:textColor="@color/default_widget_color"
        app:layout_constraintStart_toEndOf="@id/profileImgNav"
        app:layout_constraintTop_toBottomOf="@id/usernameNav" />
</androidx.constraintlayout.widget.ConstraintLayout>


